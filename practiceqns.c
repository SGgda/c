#include<stdio.h>//program to print true if its a sunday and snowing
#include<math.h>
int main(){
    int isSunday=1;
    int isSnowing=1;
    printf("%d \n", isSunday==1 && isSnowing==1);
    return 0;
}